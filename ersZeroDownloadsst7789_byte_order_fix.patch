[1mdiff --git a/App/Fonts/bdf2c_TFT.py b/App/Fonts/bdf2c_TFT.py[m
[1mindex 24a7a96..2720ea5 100644[m
[1m--- a/App/Fonts/bdf2c_TFT.py[m
[1m+++ b/App/Fonts/bdf2c_TFT.py[m
[36m@@ -71,8 +71,15 @@[m [mdef parse_bdf(path):[m
     return gl, ascent, descent[m
 [m
 def build_custom_arrays(gl, selected_codes, ascent, descent):[m
[31m-    max_glyph_height = max((g.h + g.yoff for g in gl.values()), default=ascent + descent)[m
[31m-    height = max(ascent + descent, max_glyph_height)[m
[32m+[m[32m    # top/height считаются ТОЛЬКО по глифам, реально попадающим в экспорт[m
[32m+[m[32m    # (selected_codes), а не по всему исходному BDF-файлу и не по глобальным[m
[32m+[m[32m    # FONT_ASCENT/FONT_DESCENT — иначе, например, digit-only шрифт наследует[m
[32m+[m[32m    # ascent/descent всей гарнитуры (буквы вроде p/g/y с descenders) и[m
[32m+[m[32m    # резервирует под них место, которого в выбранном наборе нет.[m
[32m+[m[32m    selected_glyphs = [gl[cp] for cp in selected_codes if cp in gl][m
[32m+[m[32m    top    = max((g.yoff + g.h for g in selected_glyphs), default=ascent)[m
[32m+[m[32m    bottom = min((g.yoff for g in selected_glyphs), default=-descent)[m
[32m+[m[32m    height = top - min(bottom, 0)[m
     bytes_per_col = (height + 7) // 8[m
     widths, xoff, yoff, dwx, off, g_h, lut = [], [], [], [], [], [], [][m
     bitmap = bytearray()[m
[36m@@ -80,7 +87,7 @@[m [mdef build_custom_arrays(gl, selected_codes, ascent, descent):[m
         g = gl.get(cp)[m
         if not g: continue[m
         lut.append(cp); off.append(len(bitmap)); widths.append(g.w); xoff.append(g.xoff)[m
[31m-        yoff.append(ascent - (g.yoff + g.h)); dwx.append(g.dwx)[m
[32m+[m[32m        yoff.append(top - (g.yoff + g.h)); dwx.append(g.dwx)[m
         r_top, r_bot = 0, -1[m
         for r in range(g.h):[m
             row_bits = any(bdf_row_bit(g, r, c) for c in range(g.w))[m
