# Screen

Экранная разметка и логика отображения, построенная поверх `App/UI` (`TextField`/`gfx`) и `App/Display`. Отвечает за то, **что и где** рисуется на экране 320×240 — сам рендеринг остаётся за `TextField_Process()`/`Gfx_Process()`.

Слой строго "только чтение" по отношению к `State`/`Settings`/`FSM` — ничего не пишет обратно, только отображает то, что там лежит (тот же принцип "UI только читает", что закреплён в `state.h`).

## Главный экран

Разметка 320×240: левая (**Solder**) и правая (**Desolder**) половины с вертикальным разделителем (`SCREEN_DIVIDER_X0/X1`, 2px), общая инфозона сверху, общая строка пресетов снизу, текущая/целевая температура по центру каждой половины.

- **Инфозона** (сверху): у правого края **своей** половины — иконка циферблата (белая, 14×14) + таймер сна канала (шрифт `AntiquaB_18_uni`, формат `MM:SS`, выровнен по правому краю у иконки); цвет текста таймера по режиму — белый (AWAKE), жёлтый (PRESLEEP), красный (SLEEP). Иконка показывается **только когда рядом есть текст таймера** — скрыта в AWAKE, пока таймер не идёт (см. `update_sleep_status()`/`s_sleep_icon_shown[]`). По центру — статус EEPROM либо сообщение об ошибке (`Error_GetInfoZoneMessage()`).
- **Заголовок канала** (title): цвет меняется активный/неактивный, и отдельно — при неисправности инструмента (`COLOR_FAULT`).
- **Температура**: текущая (крупный шрифт, `SCREEN_CURRENT_HEIGHT`) над целевой (`SCREEN_TARGET_HEIGHT`), с зазором `SCREEN_TEMP_GAP`. Поле текущей температуры (`LINE_x_CURRENT`, шрифт `Comic_60_dig`) и поле сообщения об обрыве (`LINE_x_FAULT_MSG`, шрифт `AntiquaB_18_uni`) занимают одну и ту же позицию на экране — в любой момент непусто ровно одно из двух (`update_channel_content()`): при неисправности канала (`Error_GetChannelFaultMessage()`) текст обрыва идёт через `FAULT_MSG` (кириллица, `Comic_60_dig` её не содержит); при выключенном канале (`!State_IsEnabled(ch)`) вместо числа показывается `"--"` — через `LINE_x_CURRENT` тем же крупным шрифтом `Comic_60_dig`, что и число (требует наличия дефиса в `_dig`-наборе шрифта, см. `App/Fonts/bdf2c_TFT.py`); иначе — обычное число текущей температуры.
- **Пресеты** (снизу, `SCREEN_PRESETS_Y`): три поля — левое (`TextField_Printf`), центральное (`TextField_PrintfCentered`), правое с выравниванием по правому краю (`TextField_PrintfRightAligned`) — всегда показывают пресеты **активного** канала.

## Сервисное меню

При `SCREEN_MODE_SERVICE` меню **полностью заменяет** главный экран (`render_menu()` рисуется вместо `update_channel_content()`, остальное не обновляется).

- Заголовок меню — `Menu_GetTitle()`.
- Если показывается предупреждение о входе в Expert-уровень (`Menu_IsShowingExpertWarning()`) — выводится текст предупреждения (до 3 строк), остальные строки очищаются.
- Иначе — список пунктов меню (до `SCREEN_MENU_ITEM_ROWS` = 7, максимум для уровня Expert; уровень User короче — лишние строки пустые). Для каждого пункта: `Menu_GetItemLabel()` + значение через `Menu_GetItemValueText()`.
- Цвет строки пункта: `COLOR_MENU_CURSOR` (выбран, не редактируется), `COLOR_MENU_EDITING` (выбран и редактируется прямо сейчас), `COLOR_MENU_NORMAL` (остальные).

## Функции (`screen.h`)

| Функция | Назначение |
|---|---|
| `Screen_Init()` | Вызвать один раз после `TextField_Init()`. Рисует статику (разделитель) и настраивает геометрию всех строк. Блокирует до завершения DMA-заливки разделителя (однократно, при старте, до входа в главный цикл — тот же паттерн, что и начальная заливка фона в `main.c`) |
| `Screen_Update()` | Вызывать из главного цикла. Не блокирует — обновляет только содержимое строк (`TextField_Printf`) и цвета при смене активного канала/режима экрана. Сам рендер по-прежнему продвигается отдельным вызовом `TextField_Process()` в главном цикле |

## Смена режима экрана / канала

- `s_last_screen_mode` — при смене режима экрана (`InputFSM_GetScreenMode()`) вызывается `clear_screen_for_mode_switch()`: экран физически очищается целиком (заливкой), а `TextField_InvalidateAll()` заново помечает все строки грязными — так решается переход главный экран ⟷ сервисное меню, где новый экран не обязан визуально совпадать со старым. `TextField_InvalidateAll()` стирает не только "что реально нарисовано", но и желаемый `text` каждой строки (см. `UI.md`) — а заголовки каналов (`"Паяльник"`/`"Отсос"`) печатаются `TextField_Printf()` только один раз, в `Screen_Init()`; `Screen_Update()` дальше меняет только их цвет, не текст. Поэтому при возврате в `SCREEN_MODE_MAIN` `clear_screen_for_mode_switch()` сразу ПОСЛЕ `TextField_InvalidateAll()` печатает заголовки заново — без этого они исчезали насовсем после первого же захода в сервисное меню и выхода из него (исправленный баг).
- `s_last_active_channel` — при смене активного канала (фокуса) перекрашиваются цвета title/current/target у старого и нового активного канала (`apply_channel_colors()`, `apply_target_colors()`), без полной перерисовки экрана.
- `s_last_fault[CHANNEL_COUNT]` — чтобы перекрашивать title/текущую температуру в `COLOR_FAULT` только при реальном изменении статуса неисправности, а не каждый кадр.
- `s_last_sleep_mode[CHANNEL_COUNT]` — чтобы перекрашивать текст таймера сна (белый/жёлтый/красный) только при реальной смене режима (`Sleep_GetMode()`), а не каждый кадр.
- `s_sleep_icon_x[CHANNEL_COUNT]` — x, по которому иконка циферблата РЕАЛЬНО сейчас нарисована на канал; пересчитывается на каждый вызов `update_sleep_status()` по БОЛЬШЕЙ из двух ширин — фактическая ширина НОВОГО текста таймера (`Gfx_MeasureTextWidth()`) и ширина того, что ЕЩЁ ФИЗИЧЕСКИ показано на экране (`TextField_GetShownWidth()`) — с фиксированным зазором `SLEEP_ICON_GAP_X`; не статичная позиция под "худший случай" (раньше считалась один раз в `Screen_Init()`, что могло приводить к наложению иконки и широкого текста, например "Предсон" — см. историю багов) и не просто по новой ширине (тоже приводило к багу: иконка рисуется синхронно в `Screen_Update()`, а стирание СТАРОГО, более широкого прямоугольника текста той же строки `TextField_Process()` выполняет следующим шагом того же прохода цикла — по факту показанного на экране, а не по новому тексту — и затирало свежую иконку).
- `s_sleep_icon_shown[CHANNEL_COUNT]` — реально ли иконка сейчас нарисована на экране; иконка не статична (в отличие от разделителя) — `update_sleep_status()` рисует/стирает её при каждой смене видимости (появление/исчезновение таймера), не каждый кадр. При смене режима экрана обратно на главный (`clear_screen_for_mode_switch()`) сбрасывается в `false` (экран уже очищен), чтобы ближайший `update_sleep_status()` перерисовал иконку заново при необходимости.

## Связь с гейтингом главного цикла

`Screen_Update()` вызывается из главного цикла **с** тайминг-гейтом (~30 мс, `SCREEN_UPDATE_MS`) — в отличие от `TextField_Process()`, которая гейта не имеет. См. `README.md` / `UI.md`.