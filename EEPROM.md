# EEPROM

Драйвер EEPROM. Разделён на два слоя, по тому же принципу, что и `Display`/`st7789`:

- **`eeprom.h`** — общий контракт, независимый от конкретной микросхемы. Верхний слой (`Settings`) инклюдит только этот файл и не знает, какая микросхема физически стоит и какой у неё размер страницы.
- **`eeprom_24c04.c` / `eeprom_hw.h`** — конкретная реализация для микросхемы **24C04** (I2C1). `eeprom_hw.h` приватный — адрес на шине, геометрия, тайминги — не инклюдится ничем, кроме файла реализации.

## Важно: все функции блокирующие

Запись включает ожидание внутреннего цикла записи микросхемы (poll ACK), чтение — обычный блокирующий обмен по I2C. Вызывать из главного цикла или обработчиков событий (сохранение настроек), **не из ISR** и **не с периодичностью тика UI (2 Гц)** — операции с EEPROM существенно медленнее одного кадра.

## API (`eeprom.h`)

| Функция | Назначение |
|---|---|
| `EEPROM_Init()` | Инициализировать драйвер и проверить присутствие микросхемы на шине |
| `EEPROM_WriteByte(addr, data)` | Записать один байт |
| `EEPROM_ReadByte(addr, *data)` | Прочитать один байт |
| `EEPROM_Write(addr, *data, len)` | Записать блок произвольной длины — драйвер сам разбивает запись по границам страниц микросхемы, вызывающий код может передавать блок любого размера в пределах ёмкости |
| `EEPROM_Read(addr, *data, len)` | Прочитать блок произвольной длины (последовательное чтение) |
| `EEPROM_GetSize()` | Ёмкость микросхемы в байтах |

Статусы — `EEPROM_Status_t`: `EEPROM_OK`, `EEPROM_ERROR` (ошибка обмена по I2C — NACK, шина занята и т.п., либо неверный адрес/аргументы, либо выход за пределы ёмкости), `EEPROM_TIMEOUT` (микросхема не подтвердила готовность в отведённое время — при `EEPROM_Write()` часть данных может быть уже записана).

## Реализация 24C04 (`eeprom_hw.h` / `eeprom_24c04.c`)

- **Ёмкость**: 4 Кбит = **512 байт** (`EEPROM_HW_SIZE_BYTES`).
- **Размер страницы записи**: 16 байт (`EEPROM_HW_PAGE_SIZE`, datasheet Microchip 24LC04B / Atmel AT24C04).
- **Адрес на шине**: базовый 7-битный адрес устройства `0b1010_A2A1_P0`, в 8-битном формате под HAL — `0xA0` (`EEPROM_HW_I2C_ADDR_BASE`). A1/A2 подтянуты к GND (единственная микросхема на шине).
- **Особенность 24C04**: слово адреса ячейки — 8 бит, но ёмкость 512 байт требует 9 бит адреса. Девятый (старший) бит — это бит **P0**, передаётся не в адресном байте ячейки, а в бите 1 адресного байта устройства (там, где у 24C02 был бы вывод A0). Итоговый адрес устройства собирается динамически в `eeprom_24c04.c`:
  ```c
  device_addr = EEPROM_HW_I2C_ADDR_BASE | (block << EEPROM_HW_BLOCK_BIT_POS)
  // где block = (addr >> 8) & 0x01
  ```
  Если разводка A1/A2 изменится — поправить `EEPROM_HW_I2C_ADDR_BASE`.
- **Таймаут записи**: `EEPROM_HW_WRITE_TIMEOUT_MS` = 20 мс (datasheet: tWR, максимум обычно 5 мс — взято с запасом под опрос готовности).
- **Число попыток опроса готовности**: `EEPROM_HW_READY_TRIALS` = 1 (`HAL_I2C_IsDeviceReady` за один вызов).

## Смена микросхемы

При переходе на 24C02/24C08/24C16 или FM24C64 (FRAM) — поменять значения в `eeprom_hw.h` и логику в `.c`-файле реализации; **само имя файла `eeprom_hw.h` менять не нужно**, верхний слой (`Settings`) от смены микросхемы не зависит.

## Связь с другими модулями

- Используется `Settings` для персиста конфигурации станции — см. `Settings.md`.