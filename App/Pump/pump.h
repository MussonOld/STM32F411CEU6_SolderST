/**
 * @file pump.h
 * @brief Насос отсоса (Pump_On, PB13) — включён, пока нажата кнопка насоса.
 *
 * Полярность: Pump_On активный НИЗКИЙ (низкий уровень — насос включён,
 * высокий — выключен). Начальный уровень пина — высокий (см. gpio.c/.ioc),
 * иначе насос завёлся бы на время между сбросом и Pump_Init().
 *
 * Кнопка насоса Btn_Pump (PB12) тоже активная низкая (вход с подтяжкой к
 * питанию): нажата = GPIO_PIN_RESET. Читается напрямую, без дебаунса.
 *
 * Правило: насос включён  <=>  Btn_Pump нажата
 *                              И канал отсоса включён (State_IsEnabled())
 *                              И канал отсоса не заблокирован
 *                                  (!Error_IsChannelBlocked(CHANNEL_DESOLDER)).
 * То есть насос блокируется любой неисправностью канала отсоса (RTD/нагреватель/
 * "инструмент не подключен"/код ошибки АЦП), неисправным БП (Error_IsChannelBlocked()
 * истинно для обоих каналов) и выключением канала отсоса (UP+DN). Холодное жало
 * НЕ блокирует насос, как и режим сна канала — насос от температуры не зависит.
 * Никаких таймингов (задержки, ограничения времени работы) нет — по ТЗ.
 *
 * Использование:
 *   Pump_Init() — после MX_GPIO_Init().
 *   Pump_Poll() — из главного цикла, период ~PUMP_POLL_MS.
 */

#ifndef PUMP_H
#define PUMP_H

#include <stdbool.h>

#ifdef __cplusplus
extern "C" {
#endif

/** Период опроса — общий 10 мс-гейт главного цикла (см. main.c). */
#define PUMP_POLL_MS 10U

void Pump_Init(void);
void Pump_Poll(void);

/** true — насос сейчас включён (Pump_On driven low). */
bool Pump_IsOn(void);

#ifdef __cplusplus
}
#endif

#endif /* PUMP_H */
